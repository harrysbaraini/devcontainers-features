{
    "name": "PHP-CLI",
    "id": "php-cli",
    "version": "1.0.2",
    "description": "PHP-CLI to your dev container",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "8.2",
                "8.1"
            ],
            "default": "8.2",
            "description": "PHP version to install"
        },
        "packages": {
            "type": "string",
            "proposals": [
                "imagick,gd"
            ],
            "default": "",
            "description": "List of packages to install, separated by comma and no spaces between them."
        },
        "timezone": {
            "type": "string",
            "proposals": ["UTC"],
            "default": "UTC",
            "description": "Timezone"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
